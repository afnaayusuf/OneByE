{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build:client",
  "outputDirectory": "dist/spa",
  "rewrites": [
    { "source": "/api/metrics", "destination": "/api/metrics" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
